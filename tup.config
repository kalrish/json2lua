# CONFIG_TOOLCHAIN is the toolchain of choice
#   Toolchains are defined in src/toolchains.tup.
# CONFIG_BUFFER_SIZE is the size of the code point buffer, in bytes.
#   The input buffer, used for reading, will be four times as big.
# CONFIG_DEBUG_LOGS controls whether debugging messages should be logged
